.title(class="#{title_class}") = @title
.image(style="background-image: url(#{image_url})")
  - if cta?
    .bottom.cta
      = @cta
  - if infos?
    .bottom.infos
      = @infos
  - if icons?
    .icons
      - @icons.each do |icon|
        = icon
