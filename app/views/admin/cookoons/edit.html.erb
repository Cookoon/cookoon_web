<h1 class="text-center my-4"><%= @cookoon.name %></h1>

<div class="container">
  <div class="d-flex">
    <div class="d-none d-md-block mr-2">
      <%= link_to "Retour aux cookoons", admin_cookoons_path, class: 'btn-cookoon btn-block btn-cookoon-secondary-gold mb-2 small' %>
    </div>

    <div class="container">
      <div class="d-flex">
        <div class="container">
          <div class="text-center">
            <%= carousel_for(@cookoon.photos) %>
          </div>
        </div>
      </div>
      <div class="container pt-5">
        <%#= render 'cookoons/form' %>
        <%= render 'form' %>
      </div>
    </div>
  </div>
</div>

<%= console %>
