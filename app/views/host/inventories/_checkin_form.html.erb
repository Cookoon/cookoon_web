<%= simple_form_for [:host, @reservation, @inventory], remote: true do |f| %>
  <%= f.input :checkin_photos, as: :attachinary, input_html: { data: { controller: 'attachinary' } } %>
  <div class="text-center pt-5 pb-3">
    <%= f.submit 'DEMARRER LA LOCATION', class: 'button button-blue' %>
  </div>
<% end %>
