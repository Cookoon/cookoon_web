<div class="container">
  <h2>Mes invitations</h2>
  <p>Des proches souhaitent rejoindre Cookoon ?</p>
  <p><%= t 'devise.invitations.count', count: current_user.invitation_limit %></p>

  <% if current_user.invitation_limit > 0 %>
    <%= render 'new_form' %>
  <% else %>
    <div class="py-5"></div>
  <% end %>
</div>

<div class="card-bottom mt-3">
  <div class="card-bottom-content">
    <div class="text-center">
      <i class="far fa-question-circle fa-2x" aria-hidden="true"></i>
    </div>
    <p class='mt-3'>
      Pour obtenir des invitations supplémentaires, proposez votre appartement sur Cookoon.
    </p>
    <p>
      Les évaluations positives des autres membres pourront également vous valoir des invitations supplémentaires.
    </p>
  </div>
</div>
