.container
  .row
  .offset-sm-1.col-sm-10.offset-md-2.col-md-8.offset-lg-3.col-lg-6
    h4 Convier mes invités

    - if @guests.any?
      p Mes invités déjà conviés :
      - @guests.each do |guest|
        ul
          li = guest

    - if @user_guests.any?
      p Conviez vos invités ci-dessous, nous leur envoyons un mail d'invitation de votre part :
      = render 'reservation_form'

      p.mt-4.mb-0 Votre invité n'est pas encore dans votre liste de contacts ?
    - else
      p.mt-4.mb-0 Ajoutez un nouveau contact à votre liste afin de pouvoir l'inviter.

    = render 'new_modal'
