//<%# perks.limit("#{number_of_perks}".to_i).each do |perk| %>
//<% perks.each do |perk| %>
  <i class="<%= "#{perk.icon_name}" %> text-primary"></i>
  <%= "#{perk.name}" %>&nbsp

- perks.each do |perk|
  span.d-flex.flex-nowrap
    .text-primary.pr-1 class="#{perk.icon_name}"
    span.pr-2 = perk.name
