<%= content_for :navbar_content do %>
  <h4 class='text-primary'>Mon profil</h4>
<% end %>

<div class="container">
  <%= render 'form' %>

  <div class="text-center py-3">
    <%= link_to destroy_user_session_path, method: :delete, class: 'no-button', remote: true do  %>
      <i class="fas fa-power-off" aria-hidden="true"></i> Se déconnecter
    <% end %>
  </div>
</div>
